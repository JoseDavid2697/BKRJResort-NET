@{
    Layout = "_Layout";
}

<!-- ======= Header ======= -->

<header id="header">
    <div class="container d-flex">

        <div class="logo mr-auto">
            <h1 class="text-light"><a asp-controller="Home" asp-action="Index">BKRJ Resort</a></h1>
            <!-- Uncomment below if you prefer to use an image logo -->
            <!-- <a href="index.html"><img src="assets/img/logo.png" alt="" class="img-fluid"></a>-->
        </div>

        <nav class="nav-menu d-none d-lg-block">
            <ul>
                <li class="active"><a asp-controller="Home" asp-action="Index">Home</a></li>
                <li><a asp-controller="Home" asp-action="SobreNosotros">Sobre Nosotros</a></li>
                <li><a asp-controller="Home" asp-action="Facilidades">Facilidades</a></li>
                <li><a asp-controller="Home" asp-action="ComoLlegar">¿Cómo Llegar?</a></li>
                <li><a asp-controller="Home" asp-action="Tarifas">Tarífas</a></li>
                <li><a asp-controller="Home" asp-action="ReservasEnLinea">Reservas en Línea</a></li>
                <li><a asp-controller="Home" asp-action="Contactenos">Cóntactenos</a></li>

            </ul>
        </nav><!-- .nav-menu -->

    </div>
</header><!-- End Header -->


<main id="main">

    <!-- ======= Breadcrumbs ======= -->
    <section id="breadcrumbs" class="breadcrumbs">
        <div class="container">

            <div class="d-flex justify-content-between align-items-center">
                <h2>¿Cómo llegar?</h2>
                <ol>
                    <li><a asp-controller="Home" asp-action="Index">Home</a></li>
                    <li>¿Cómo llegar?</li>
                </ol>
            </div>
    </section>
    <br>

    <div class="section-title" data-aos="fade-up">
        <h2>Tenemos una ubicacion <strong>estratégica</strong> para el disfrute del mar caribe</h2>
        <p>
            BKRJ Resort, situado en Puntarenas, a 1,2 km del parque marino del Pacífico, ofrece alojamiento con restaurante, aparcamiento privado gratuito, piscina al aire libre y bar. A 400 m del estadio Lito Pérez, el hotel cuenta con un jardín y una terraza. Hay wifi gratis y servicio de habitaciones.
        </p>
    </div>
    <!-- ======= MAP ======= -->
    <section id="portfolio" class="portfolio">
        <div class="container">
            <div class="row justify-content-md-center">
                <div class="col-md-10">
                    <div id='map' style='height:480px; width: 1000px;'>
                    </div>
                    <script>
                        var map = L.map('map').setView([9.977213, -84.830318], 4);
                        var redIcon = new L.Icon({
                            iconUrl: '../assets/img/marker-icon-2x-red.png',
                            iconSize: [25, 41],
                            iconAnchor: [12, 41],
                            popupAnchor: [1, -34],
                            shadowSize: [41, 41]
                        });
                        var marker1 = L.marker([9.977213, -84.830318], { icon: redIcon }).addTo(map);
                        var latlngs = Array();

                        //Get latlng from first marker
                        latlngs.push(marker1.getLatLng());

                        

                        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                            attribution: '&copy; <a href="https://osm.org/copyright">OpenStreetMap</a> contributors'
                        }).addTo(map);

                        var searchControl = L.esri.Geocoding.geosearch().addTo(map);

                        var results = L.layerGroup().addTo(map);

                        searchControl.on('results', function (data) {
                            let marker2;
                            results.clearLayers();
                            for (var i = data.results.length - 1; i >= 0; i--) {
                                marker2 = L.marker(data.results[i].latlng);
                                results.addLayer(marker2);
                                
                            }

                            //Get latlng from first marker
                            latlngs.push(marker2.getLatLng());

                            //You can just keep adding markers

                            //From documentation http://leafletjs.com/reference.html#polyline
                            // create a red polyline from an arrays of LatLng points
                            var polyline = L.polyline(latlngs, { color: 'green' }).addTo(map);

                            // zoom the map to the polyline
                            map.fitBounds(polyline.getBounds());

                        });
                    </script>
                </div>
            </div>


        </div>

    </section>




</main><!-- End #main -->